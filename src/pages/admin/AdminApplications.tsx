import { useEffect, useMemo, useState } from "react";
import { Link } from "react-router-dom";
import AdminLayout from "../../components/admin/AdminLayout";
import { listAdminApplications } from "../../services/careerService";
import { applicationStatusLabels, type AdminApplication, type ApplicationStatus } from "../../types/career";

const filterOptions: Array<["all" | ApplicationStatus, string]> = [["all", "すべて"], ["submitted", "応募済み"], ["reviewing", "確認中"], ["interview", "面接"], ["accepted", "合格"], ["rejected", "不合格"], ["withdrawn", "辞退"]];
export default function AdminApplications() { const [items, setItems] = useState<AdminApplication[]>([]); const [filter, setFilter] = useState<"all" | ApplicationStatus>("all"); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); useEffect(() => { void listAdminApplications().then(setItems).catch(() => setError("応募情報を取得できませんでした。" )).finally(() => setLoading(false)); }, []); const visible = useMemo(() => items.filter((item) => filter === "all" || item.status === filter), [filter, items]); return <AdminLayout title="応募者管理"><div className="adminToolbar"><p>応募内容の確認と選考ステータスの管理を行います。</p></div><div className="adminFilters">{filterOptions.map(([value, label]) => <button className={filter === value ? "isActive" : ""} onClick={() => setFilter(value)} key={value}>{label}<span>{value === "all" ? items.length : items.filter((item) => item.status === value).length}</span></button>)}</div>{loading ? <div className="careerState">読み込んでいます...</div> : error ? <div className="careerState isError">{error}</div> : visible.length === 0 ? <div className="careerState">該当する応募はありません。</div> : <div className="adminTableWrap"><table className="adminTable"><thead><tr><th>応募者</th><th>応募先求人</th><th>企業名</th><th>応募日</th><th>卒業予定年</th><th>ステータス</th><th></th></tr></thead><tbody>{visible.map((item) => <tr key={item.id}><td><strong>{item.applicant_name}</strong></td><td>{item.internship?.title}</td><td>{item.internship?.company_name}</td><td>{new Intl.DateTimeFormat("ja-JP").format(new Date(item.created_at))}</td><td>{item.graduation_year}</td><td><span className={`statusBadge is-${item.status}`}>{applicationStatusLabels[item.status]}</span></td><td><Link to={`/admin/applications/${item.id}`}>詳細</Link></td></tr>)}</tbody></table></div>}</AdminLayout>; }
